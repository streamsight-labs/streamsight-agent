// JAAS configuration for Kafka with SCRAM-SHA-512
// Used by KRaft controller and broker

KafkaServer {
    org.apache.kafka.common.security.scram.ScramLoginModule required
    username="admin"
    password="admin-secret";
};
