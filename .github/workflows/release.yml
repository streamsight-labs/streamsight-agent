name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
            ext: .exe

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          # Same toolchain as CI and the Dockerfile builder.
          go-version-file: go.mod
          cache: true

      # The tag verbatim, which is also what the Makefile's `git describe`
      # produces, stamped into main.version with the same ldflag. It travels
      # on every batch as agent_version.
      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "building ${VERSION} for ${GOOS}/${GOARCH}"
          go build -trimpath -ldflags="-s -w -X main.version=${VERSION}" \
            -o streamsight-agent-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }} \
            ./cmd/agent

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: streamsight-agent-${{ matrix.goos }}-${{ matrix.goarch }}
          path: streamsight-agent-*
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      # Only THIRD_PARTY_NOTICES.txt is wanted from the source tree, but it has
      # to be fetched before the artifacts are downloaded: actions/checkout
      # cleans the workspace it checks out into and would take artifacts/ with
      # it. This job builds nothing, so the clone costs a few seconds and no
      # toolchain setup.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: artifacts
          merge-multiple: true

      # The notices join the binaries in artifacts/, which is what `files:`
      # below globs, so they are attached to the release rather than left in a
      # repository the person downloading a binary never visits. Every
      # dependency linked into these binaries is BSD-3-Clause, whose second
      # clause requires exactly that: the copyright notices reproduced "in the
      # documentation and/or other materials provided with the distribution".
      # They are checksummed alongside the binaries because a notice file that
      # can be swapped without detection is not much of a notice.
      - name: Checksums
        run: |
          cp THIRD_PARTY_NOTICES.txt artifacts/
          cd artifacts
          sha256sum streamsight-agent-* THIRD_PARTY_NOTICES.txt > SHA256SUMS
          cat SHA256SUMS

      - name: Create Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          files: artifacts/*
          generate_release_notes: true
